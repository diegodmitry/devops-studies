# Server Performance Stats

A Bash script to collect Linux server performance and system health metrics.

This project is part of my DevOps learning journey, following the roadmap.sh DevOps path.

- Roadmap.sh project: https://roadmap.sh/projects/server-stats
- Level: Beginner
- Main topics: Linux, Bash, Shell Scripting, Server Performance, Troubleshooting

## Goal

The goal of this project is to create a script called `server-stats.sh` that can be executed on a Linux server to collect basic performance and system health information.

This kind of script is useful during the first investigation of a slow or unstable server, especially in support, infrastructure, cloud, and DevOps environments.

## Requirements from roadmap.sh

The original roadmap.sh project asks for a script that shows:

- Total CPU usage
- Total memory usage, including free vs used and percentage
- Total disk usage, including free vs used and percentage
- Top 5 processes by CPU usage
- Top 5 processes by memory usage

Stretch goals:

- OS version
- Uptime
- Load average
- Logged-in users
- Failed login attempts

## Implemented features

This script currently shows:

- Hostname
- Report generation date and time
- Operating system information
- Kernel version
- System architecture
- Uptime
- Load average
- Total CPU usage
- Number of CPU cores
- Memory usage
- Swap usage
- Disk usage by filesystem
- Top 5 processes by CPU usage
- Top 5 processes by memory usage
- Logged-in users
- Failed login attempts in the last 24 hours
- Color highlighting for easier reading

## Technologies used

- Bash
- Linux CLI
- `top`
- `free`
- `df`
- `ps`
- `awk`
- `grep`
- `sort`
- `who`
- `journalctl`
- `bc`
- `/proc/loadavg`
- `/etc/os-release`

## Project structure

```text
server-stats/
├── README.md
├── server-stats.sh
└── assets/
    └── server-stats-output.png
```

## How to run

Clone the repository:

```bash
git clone https://github.com/diegodmitry/devops-studies.git
```

Go to the project folder:

```bash
cd devops-studies/Beginner/server-stats
```

Give execution permission:

```bash
chmod +x server-stats.sh
```

Run the script:

```bash
./server-stats.sh
```

Or run it directly with Bash:

```bash
bash server-stats.sh
```

## Screenshot

Below is an example of the report generated by the script:

<p align="center">
  <img src="./assets/image.png" alt="Server Performance Stats terminal output showing Linux system information, CPU usage, memory usage, disk usage, top processes, logged-in users, and failed login attempts" width="650">
</p>

## Real-world scenario

A user reports that an application is slow.

Before escalating the issue, a support engineer can run this script to quickly check:

- Is the CPU usage high?
- Is the server running out of memory?
- Is the disk almost full?
- Is any process consuming too many resources?
- Is the load average abnormal?
- Are there suspicious failed login attempts?

This helps with the first technical analysis before deeper investigation.

## What I practiced

With this project, I practiced:

- Reading Linux system metrics
- Writing Bash scripts
- Using Linux troubleshooting commands
- Formatting terminal output
- Working with CPU, memory, disk, and process information
- Checking basic security signals
- Building reusable operational scripts
- Thinking like a support/cloud/DevOps engineer during incident analysis

## Requirements

This script is intended to run on Linux.

Recommended tools available on the system:

- `bash`
- `top`
- `free`
- `df`
- `ps`
- `awk`
- `grep`
- `sort`
- `who`
- `journalctl`
- `bc`

Some features may vary depending on the Linux distribution and available logs.

For failed login attempts, the script tries to use:

- `journalctl`
- `/var/log/auth.log`
- `/var/log/secure`

## Current limitations

- The script focuses on local server analysis only.
- It does not export results to a file yet.
- It does not send alerts.
- It does not support command-line options yet.
- It assumes common Linux command output formats.
- The current terminal output is in Portuguese.

## Possible improvements

Future improvements:

- Translate the terminal output to English
- Add command-line flags, for example `--json`, `--no-color`, `--output`
- Export the report to a log file
- Add JSON output
- Add configurable alert thresholds
- Add ShellCheck validation
- Add automated testing
- Add Docker-based test environment
- Add support for remote server execution
- Add Prometheus-friendly output format

## Status

Completed.

This project covers the main roadmap.sh requirements and also includes the suggested stretch goals.

## Reference

- https://roadmap.sh/projects/server-stats